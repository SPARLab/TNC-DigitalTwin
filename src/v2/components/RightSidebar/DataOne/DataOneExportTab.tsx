// ============================================================================
// DataOneExportTab — Placeholder export guidance for DataOne.
// NOTE: Right sidebar export tab removed globally (DFT-041).
// ============================================================================

export function DataOneExportTab() {
  return (
    <div id="dataone-export-tab" className="space-y-2">
      <p id="dataone-export-tab-text" className="text-sm text-gray-600">
        Export actions are available from the global Export Builder in the header.
      </p>
    </div>
  );
}
